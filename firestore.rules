rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // النظام يعتمد على Admin SDK في السيرفر — أغلق الوصول المباشر من المتصفح
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
